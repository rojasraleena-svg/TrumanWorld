[build]
builder = "RAILPACK"
buildCommand = "mkdir -p /app/.uv-cache && cd backend && UV_CACHE_DIR=/app/.uv-cache uv sync --frozen"
watchPatterns = [
  "/backend/**",
  "/agents/**",
  "/scenarios/**",
  "/scripts/**",
]

[deploy]
preDeployCommand = "cd backend && uv run alembic upgrade head"
startCommand = "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
