[build]
builder = "RAILPACK"
buildCommand = "mkdir -p /app/.uv-cache && cd backend && UV_CACHE_DIR=/app/.uv-cache uv sync --frozen"
watchPatterns = [
  "/backend/**",
  "/agents/**",
  "/scenarios/**",
  "/scripts/**",
]

[deploy]
preDeployCommand = ""
startCommand = "echo '=== STARTING BACKEND ===' && pwd && ls /app/backend/ && cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
